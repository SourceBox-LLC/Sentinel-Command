{{ notification.title }}

{{ notification.body }}

{% if notification.camera_id -%}
Camera ID: {{ notification.camera_id }}
{% endif -%}

What this might mean:
  - The camera lost power or was unplugged.
  - The CameraNode hosting it crashed or was restarted.
  - Network connectivity between the node and Command Center dropped.

View this camera: {{ dashboard_url }}{{ notification.link or '/dashboard' }}

——
You'll get a follow-up email when the camera comes back online.
Manage email alerts: {{ dashboard_url }}/settings#settings-notifications
Unsubscribe from camera-offline alerts: {{ unsubscribe_url }}
