# Never bake local secrets or VCS state into the image.  `COPY . .` in
# the Dockerfile would otherwise ship .env (which can hold a live
# OLLAMA_API_KEY) into an image layer on any plain `docker build`.
.env
.env.*
.git
.github
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.venv
